# Local development defaults. Nothing in here is secret; real deployments set
# these in the environment. Scripts and the dev server load this file via
# `node --env-file`.
DATABASE_URL=postgresql://ubuntu:postgres@localhost:5432/internal_tools
TEST_DATABASE_URL=postgresql://ubuntu:postgres@localhost:5432/internal_tools_test
AUTH_PROVIDER=mock
